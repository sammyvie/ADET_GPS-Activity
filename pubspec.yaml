name: mapmeapp
description: "GPS Map Activity"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_map: 3.1.0
  latlong2: 0.8.1
  geolocator: 9.0.2
  cupertino_icons: 1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
